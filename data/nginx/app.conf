server {
    listen 80;
    server_name wordpress;
    location / {
        return 301 https://$host$request_uri;
    }    
}
server {
    listen 443 ssl;
    server_name wordpress;
    
    location / {
        proxy_pass wordpress; #for demo purposes
    }
}
